#include <stdio.h>
/**
*main - entry point
*@void: takes no arguments
*Return: int
*
*/
int main(void)
{char ch0 = 'z';
char ch1 = '\n';
for (ch0 = 'z' ; ch0 >= 'a' ; ch0--)
{putchar(ch0); }
{putchar(ch1); }
return (0); }
